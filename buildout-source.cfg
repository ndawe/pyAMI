[buildout]
extends = buildout.cfg buildout-versions.cfg
parts = scripts ami-test buildout-source-release
versions = versions
extensions =
relative-paths = true

[buildout-source-release]
recipe = zc.recipe.egg:scripts
eggs = zc.sourcerelease

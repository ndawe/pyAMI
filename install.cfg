[buildout]
parts = eggs scripts
allow-picked-versions = false
versions = versions
download-cache = cache
relative-paths = true

[eggs]
recipe = zc.recipe.egg
interpreter = python
eggs = pyAMI

[scripts]
recipe = zc.recipe.egg:scripts
eggs = pyAMI

